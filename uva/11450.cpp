#include <iostream>
#include <vector>
using namespace std;

int N, M, C, A[20][20], i, mem[200][20];
vector<vector<int>> a;

int main(){
    
    scanf("%d", &N);

    while(N--){
        memset(A, -1, sizeof(A));
        memset(mem, -1, sizeof(mem));

        scanf("%d %d", &M, &C);
        for(i = 0; i <C; i++){
            scanf("%d", &K);
            for(j = 0 ; j< K; j++){
                scanf("%d", A[i][j]);
            }
        }
        
        

        //sort the vectors
        //build the mem array from bottom up 
        result = 0;
        while(M != result){
            

        }
        result = tryBuy(
    }

}


